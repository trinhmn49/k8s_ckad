apiVersion: v1
kind: Service
metadata:
  name: user-service-demo
  namespace: weather-alert
spec:
  selector:
    app: user-service-demo
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP
